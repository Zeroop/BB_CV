const express = require('express');
const router = express.Router();
const LoginController = require('../app/controllers/LoginController');
const dataQuery = require;
router.use(LoginController.index);

module.exports = router;